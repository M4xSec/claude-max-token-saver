---
description: Let the next tool call return full unfiltered output
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/max_token_saver.py" full` to disable filtering for the next tool call.
Report confirmation to the user.
